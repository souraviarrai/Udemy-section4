<!DOCTYPE html>
<html>
<head>
  <title>All Users</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
<div class="container">
  <h1>All users</h1>

  <table class="table table-striped table-hover">
    <thead class="thead-dark">
    <tr>
      <th scope="col">Username</th>
      <th scope="col">Articles</th>
      <th scope="col">Actions</th>
      <th scope="col">Joined</th>
    </tr>
    </thead>
    <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= link_to user.username, user, class: "text-decoration-none" %></td>
        <td><%= pluralize(user.articles.count, "article") %></td>
        <td>
          <%= link_to "View Profile", user, class: "btn btn-primary btn-sm me-2" %>
          <%= link_to "Edit Profile", edit_user_path(user), class: "btn btn-secondary btn-sm" %>
        </td>
        <td>Joined <%= time_ago_in_words(user.created_at) %> ago</td>
      </tr>
    <% end %>
    </tbody>
  </table>

  <div class="flickr_pagination">
    <%= will_paginate @user, :container => false %>
  </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/js/bootstrap.bundle.min.js"></script>
</body>
</html>
